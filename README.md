# Cyber-Security-Project---DD26


## Getting started
- Create a copy of the .env.example, rename it to .env and fill in the required values
- run `npm i` at the base directory to install all dependencies
- run `npm run dev` at the base directory to run both the backend and frontend